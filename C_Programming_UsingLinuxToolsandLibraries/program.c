    #include <stdio.h>
    #include <math.h>
    /// double pow(double, double); 
    int main(){
        double a = 2.5;
        double asquared = pow(a,2);
        printf("%.2lf squared = %.2lf\n",a,asquared);
        return 0;
    }

